# Pitfalls

> **TL;DR:** Distilled, current-truth gotchas — read before risky work in this repo. Populated by the distillation pass and maintained from the [lessons log](lessons-log.md).

Format per entry: the trap, the symptom it produces, the rule that avoids it, and its evidence label (✅/❓).

<!-- TODO(distillation): fill from git history themes, code reading, and incident knowledge.
     Seed questions: which bugs recur in commit subjects? which modules carry the most fix
     churn? which commands have nondeterministic or slow behavior worth warning about? -->
