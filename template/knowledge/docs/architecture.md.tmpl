# Architecture

> **TL;DR:** {{ARCH_TLDR}}
> **Read when:** orienting in the repo; asking how systems connect. **Skip when:** per-service detail — see the subsections once they exist.

**Verification baseline:** commit `{{BASELINE_SHORT}}` (`{{BASELINE_FULL}}`), {{DATE}}. Labels: ✅ VERIFIED · ❓ UNVERIFIED.

<!-- TODO(distillation): replace this stub with the verified architecture — stack tiers,
     how components connect, data flow, entry points. Start from the evidence pack in
     knowledge/_bootstrap-brief.md; every claim gets ✅ + file evidence or ❓. -->
